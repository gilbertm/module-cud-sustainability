{#
/**
 * @file
 * Base fallback template for CUD Sustainability pages.
 *
 * Drupal uses this template when no variant-specific file is found.
 * Template suggestions (highest priority last):
 *   cud_sustainability__normal  → cud-sustainability--normal.html.twig
 *   cud_sustainability__main    → cud-sustainability--main.html.twig
 *   cud_sustainability__{slug}  → cud-sustainability--{slug}.html.twig
 *
 * Variables:
 * - title:         Page title string.
 * - content:       Rendered node content (full view mode).
 * - variant:       Template variant derived from URL slug.
 * - carousel_items: Optional list of carousel slides.
 * - sections:      Optional list of processed-text sections.
 * - primary_nav:   Primary navigation array.
 * - secondary_nav: Secondary navigation array.
 */
#}
{{ content }}
