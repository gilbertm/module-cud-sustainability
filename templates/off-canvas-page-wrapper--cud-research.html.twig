{#
/**
 * @file
 * Route-specific off-canvas wrapper override for CUD Research.
 *
 * Intentionally renders children directly to avoid injecting the
 * dialog-off-canvas-main-canvas wrapper container on research routes.
 */
#}
{{ children }}
